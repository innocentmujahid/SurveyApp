<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">#FFD00B</item>
        <item name="colorControlActivated">#FFD00B</item>
    </style>


    <style name="MyRatingBar" parent="@android:style/Widget.DeviceDefault.Light.RatingBar.Indicator">
        <item name="android:minHeight">40dp</item>
        <item name="android:maxHeight">60dp</item>
        <item name="android:minWidth">30dp</item>
        <item name="android:maxWidth">70dp</item>
    </style>

</resources>
